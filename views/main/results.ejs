<%- include('../partials/head') %>

<link rel='stylesheet' href='/stylesheets/social.css' />
<link rel='stylesheet' href='/stylesheets/header.css' />
<link rel='stylesheet' href='/stylesheets/listing.css' />
<link rel='stylesheet' href='/stylesheets/footer.css' />
<link rel='stylesheet' href='/stylesheets/common.css' />

</head>

<body>
    <%- include('../partials/social') %>
    <%- include('../partials/header') %>
    <section class="section">
        <div>
            <div>
                <h2>Resultados de la busqueda: <%= search %></h2>
            </div>
            
                <div class="d-flex f-row mar-10">
                <div class="w-80">
                    <div>
                      <div class="d-flex f-row f-wrap j-arround">
                        <% if(product.length > 0) { %>
                            <% product.forEach(product => { %>
                          
                        <%- include('../partials/productListings') %>
                        <% }); %>
                      </div>
                    </div>
                  </div>
                </div>
            
            <% } else {%>
            <div>
                <h2>No hay productos</h2>
            </div>
            <% } %>
        </div>
    </section>
    <%- include('../partials/footer') %>
</body>

</html>